/**
 * Represents the release context that used to handle the process information.
 */
export interface GitHubReleaseActionContext {
  /**
   * Represents the GitHub release id.
   */
  releaseId?: number;
}
